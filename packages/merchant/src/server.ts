import { logConfig } from '@rwa/shared'

// Phase 0 placeholder. Replaced in Phase 1 by the full MPP charge server.
function main(): void {
  logConfig('merchant')
  console.log('merchant server not yet implemented (Phase 1)')
}

main()
